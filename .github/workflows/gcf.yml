name: Go

on:
  push:
    branches: [main]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Set up Go 1.x
        uses: actions/setup-go@v2
        with:
          go-version: ^1.13
        id: go

      - name: Check out code into the Go module directory
        uses: actions/checkout@v2

      - name: Get dependencies
        run: go mod download

      - name: Build
        run: go build -v .

      - name: Test
        run: go test ./... -v .

  deploy:
    name: deploy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: google-github-actions/setup-gcloud@v0.2.1
        with:
          service_account_key: ${{ secrets.GCP_SA_KEY }}
          export_default_credentials: true
      - id: Deploy getMessage function
        uses: google-github-actions/deploy-cloud-functions@main
        with:
          name: getMessage
          entry_point: GetMessage
          runtime: go113

      - id: Deploy hello function
        uses: google-github-actions/deploy-cloud-functions@main
        with:
          name: hello
          entry_point: Hello
          runtime: go113